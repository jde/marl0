FROM node:18-slim

WORKDIR /app

# Install dependencies (optional placeholder)
# COPY package.json package-lock.json ./
# RUN npm install

# Default command (optional placeholder)
CMD ["node", "index.js"]
